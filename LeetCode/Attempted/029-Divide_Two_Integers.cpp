#include <iostream>
using namespace std;

class Solution {
public:
    int divide(int dividend, int divisor) {
        int cnt = 0;
        while(dividend >= 0) {
            dividend -= divisor; 
        }
    }
}
